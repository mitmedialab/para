/*ConstraintMap.js
* class that stores map of all constraint 
*objects for access and update
*/

define([
	'underscore',
	'backbone',
	'models/data/Instance'


], function(_, Backbone, Instance) {

var ConstraintMap = Backbone.Model.extend({

		defaults: {},

		});


return ConstraintMap;
});